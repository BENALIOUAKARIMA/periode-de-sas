#include <stdio.h>

int main()
{
    int a, b, c;
    float s, s1, s2, s3;
    printf("entrer a, b, c ");
    scanf("%d%d%d",&a,&b,&c);
    if(a=0)
    if(b=0)
    if(c=0)
    printf("")

    return 0;
}